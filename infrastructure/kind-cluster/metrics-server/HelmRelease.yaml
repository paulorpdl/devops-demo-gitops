apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: metrics-server
spec:
  interval: 10m
  timeout: 5m
  chart:
    spec:
      chart: metrics-server
      version: '6.5.*'
      sourceRef:
        kind: HelmRepository
        name: metrics-server
      interval: 5m
  releaseName: metrics-server